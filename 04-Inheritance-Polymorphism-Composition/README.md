# 04 · Herencia, polimorfismo y composición

## Cómo compilar y correr

Hay un `Makefile` en `ejemplos/` con un target por ejemplo:

```bash
cd ejemplos
make run      # ejemplo 01
make run-02   # ejemplo 02
make run-03   # ejemplo 03
make clean    # borra las carpetas out generadas
```